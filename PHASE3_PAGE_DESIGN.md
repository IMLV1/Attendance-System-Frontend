# Phase 3 — ออกแบบ layout รายหน้าบน tablet / desktop

> เอกสารข้อเสนอ เขียนจากสิ่งที่เห็นจริงตอนไล่ดูครบ 18 หน้า (งาน 0.4, 24 ส.ค.)
> ยังไม่ได้ลงมือสักหน้า — ตัวเลขความกว้างที่เสนอเป็นจุดตั้งต้นให้เถียง ไม่ใช่ข้อสรุป
> แผนหลักอยู่ที่ [RESPONSIVE_PLAN.md](RESPONSIVE_PLAN.md)

---

## ปัญหาร่วมที่เจอเกือบทุกหน้า

**แถวเนื้อหากว้างเต็ม 1100 แต่มีของอยู่แค่ซ้ายสุดกับขวาสุด**

เกิดจากเอาเลย์เอาต์ที่ออกแบบมาสำหรับจอกว้าง ~390 มายืดตรงๆ บนมือถือ ป้ายกับค่า
ห่างกัน ~250px ตายังกวาดถึง พอยืดเป็น 1100 ระยะกลายเป็น ~950px สายตาเชื่อมสองฝั่ง
ไม่ติด กลายเป็นแถวว่างที่มีของลอยอยู่สองขอบ

**นี่ไม่ใช่บั๊กรายหน้า** — แก้ทีละหน้าจะได้ 18 วิธีแก้ที่ไม่เหมือนกัน ต้องตั้งกติกาก่อน

---

## กติกาที่เสนอ (ตัดสิน 4 ข้อนี้ก่อนลงมือ)

### 1. เลิกใช้ความกว้างเดียว 1100 ทุกหน้า → แยกตาม "รูปทรง" ของหน้า

`Responsive.contentMaxWidth = 1100` ตอนนี้ใช้กับทุกหน้าเหมือนกันหมด เสนอให้แยก:

| รูปทรง | ความกว้าง | ใช้กับ |
|---|---|---|
| `form` | **600** | หน้ากรอกฟอร์ม, หน้าตั้งค่าที่มีไม่กี่ช่อง |
| `list` | **800** | รายการที่มีบรรทัดเดียวต่อแถว |
| `dashboard` | **1100** | หน้าที่มีการ์ด/กราฟหลายก้อนวางเรียงกันจริงๆ |

800 ทำให้ป้ายกับค่าห่างกันราว 650px ซึ่งยังกวาดตาถึง และ 600 สำหรับฟอร์มคือ
ความกว้างที่บรรทัดข้อความยังอ่านสบาย

> ⚠️ ตัวเลขพวกนี้ผมเดาจากสายตาบนภาพหน้าจอ ไม่ได้วัดกับผู้ใช้จริง — ถ้าคุณมี
> ความเห็นว่าควรกว้าง/แคบกว่านี้ เชื่อคุณมากกว่า

### 2. ห้ามตรึงปุ่มไว้ขอบล่างจอในโหมด `expanded`

บนมือถือปุ่ม "ส่ง" ติดขอบล่าง = นิ้วโป้งเอื้อมถึงพอดี บน desktop จอสูง 830
ปุ่มไปอยู่ห่างจากฟอร์ม 600px กลายเป็นของที่หาไม่เจอ
→ โหมด `expanded` ให้ปุ่มอยู่ **ต่อจากเนื้อหาเลย**

### 3. ตัวควบคุมที่มีความกว้างตามธรรมชาติ ห้ามยืด

วงล้อเลือกเดือน/เวลา, ปฏิทิน, ช่องลายเซ็น — พวกนี้มีขนาดที่ "ถูก" ของมันอยู่แล้ว
ยืดเต็มความกว้างแล้วพัง (ชื่อเดือนกับตัวเลขไปอยู่คนละฝั่งจอ)
→ กำหนดความกว้างตายตัวแล้วจัดกลาง

### 4. แถวที่มีของแค่สองขอบ ต้องเลือกอย่างใดอย่างหนึ่ง

- **หด** ให้แคบลงตามกติกาข้อ 1 หรือ
- **เติมของตรงกลาง** ที่มีประโยชน์จริง (บนจอกว้างมีที่พอจะโชว์ข้อมูลที่มือถือซ่อนไว้)

ห้ามปล่อยว่าง

---

## รายหน้า

### 🔴 ต้องออกแบบใหม่จริงๆ

#### `/check-in` — ช่องว่างกลางจอ
**ที่เห็น**: `spaceBetween` ดันการ์ดนาฬิกาไปบนสุด การ์ด "สถานะปัจจุบัน" ไปล่างสุด
เหลือช่องว่าง ~300px กลางจอ วงกลมเช็คอินลอยอยู่ตรงกลางความว่างนั้น

**เสนอ**: คอลัมน์เดียวกว้าง ~560 จัดกึ่งกลางแนวตั้ง เว้นระยะคงที่ระหว่างก้อน
(ไม่ใช้ `spaceBetween`) วงกลมเช็คอินเป็นพระเอก การ์ดสถานะอยู่ใต้มันเลย

**ทางเลือกที่ 2**: สองคอลัมน์ — ซ้ายคือนาฬิกา+วงกลมเช็คอิน ขวาคือสถานะวันนี้+
ประวัติล่าสุด ใช้พื้นที่คุ้มกว่าแต่เปลี่ยนโครงหน้าเยอะกว่า

#### `/settings/budget-year` — วงล้อยืดเต็มจอ
**ที่เห็น**: วงล้อเลือกเดือนกว้าง 1100 ชื่อเดือนชิดขวาของครึ่งซ้าย ตัวเลขชิดซ้าย
ของครึ่งขวา ห่างกันเกือบทั้งจอ

**เสนอ**: การ์ดกว้าง ~480 จัดกลาง วงล้อกว้าง ~320 ข้างในนั้น (กติกาข้อ 3)
ทั้งหน้ามีฟิลด์เดียว ไม่ต้องกินพื้นที่ทั้งจอ

#### `/leave-request/create` · `/attendance-request/create` — ปุ่มส่งลอยห่าง
**ที่เห็น**: ฟอร์มจบที่ y≈160 ปุ่ม "ส่ง" อยู่ y≈720 ห่างกัน 560px

**เสนอ**: ฟอร์มกว้าง 600 จัดกลาง ปุ่มส่งต่อจากฟอร์มเลย (กติกาข้อ 2)

#### `/profile` — การ์ดรูปเป็นแถบขาวเปล่า
**ที่เห็น**: การ์ดบนสุดกว้าง 1100 สูง ~70 มีแค่วงกลมอักษรย่อเล็กๆ ชิดซ้าย
ที่เหลือขาวโล่ง · ฟิลด์ด้านล่างป้ายซ้าย–ค่าขวา ห่างกันเต็มจอ

**เสนอ**: สองคอลัมน์กว้างรวม ~900 — ซ้ายเป็นการ์ดรูป+ชื่อ+ป้ายตำแหน่ง (แนวตั้ง)
ขวาเป็นรายการฟิลด์ ทำให้การ์ดรูปมีเหตุผลที่จะมีอยู่

---

### 🟡 หด/จัดใหม่ ตามกติกา ไม่ต้องออกแบบเฉพาะตัว

| หน้า | ทำอะไร |
|---|---|
| `/attendance-history` | รายการวัน → กว้าง 800 **หรือ** grid 2 คอลัมน์ (แต่ละวันเป็นการ์ด) ซึ่งใช้พื้นที่คุ้มกว่าและยังอ่านเป็นไทม์ไลน์ได้ |
| `/approval` | รายการคำขอ → กว้าง 800 (แถบแท็บแก้ไปแล้ว) |
| `/leave-request` · `/attendance-request` | เหมือนกัน → 800 |
| `/notification` | รายการแจ้งเตือน → 800 |
| `/settings/config-*` (3 หน้า) | รายการตั้งค่า → **600** ป้ายกับสวิตช์จะอยู่ใกล้กันพอมอง |
| `/settings/role-management` | รายการกลุ่ม → 800 หรือ grid 2 คอลัมน์ |
| `/settings/user-management` | ดูข้อ Phase 2 ข้างล่าง — ถ้ายังไม่ทำ master-detail ก็หดเป็น 800 ไปก่อน |
| `/statistic` | ดูหัวข้อแยกข้างล่าง — เป็นหน้าเดียวที่ควรเป็น **2 คอลัมน์จริงๆ** |

---

### 📊 `/statistic` — จัดเป็น 2 คอลัมน์

> ⚠️ **แก้จากที่เขียนไว้ตอนแรก** — รอบแรกผมสรุปว่า "ใช้พื้นที่คุ้มอยู่แล้ว คงไว้ 1100"
> พอไล่ดูละเอียดอีกรอบพบว่าผิด หน้านี้เป็นโรคเดียวกับหน้าอื่น แค่กลบด้วยจำนวนก้อนที่เยอะ

**ที่เห็น**

| ก้อน | ปัญหา |
|---|---|
| การ์ด "การลางาน" | ชื่อประเภทลาอยู่ x≈600 ตัวเลขอยู่ x≈1360 — **ห่างกัน 660px** หนักสุดในหน้า |
| การ์ด "อัตราการเข้างาน" | ป้าย/จำนวนวัน/เปอร์เซ็นต์ แยกเป็น 3 คอลัมน์ห่างกัน ~370px ต่อช่วง |
| ตัวเลขสรุป 4 ตัว | กระจายอยู่คนละที่ — 2 ตัวบนสุด (248 วัน, 239 วัน) อีก 2 ตัวล่างสุด (1840.4 ชม., 920.2 ชม.) ต้องเลื่อนจอถึงจะเห็นครบ |
| กราฟชั่วโมงทำงาน | มีแค่ 2 แท่ง กระจายอยู่ในพื้นที่กว้าง ~920px แท่งเล็กจ้อยลอยอยู่กลางความว่าง |
| แกน Y ของกราฟ | ตัวเลขตัดบรรทัด "1,282" → "1,28"/"2" (ความกว้างที่จองไว้แคบไป) |

**เสนอ**

```
┌──────────┬──────────┬──────────┬──────────┐
│ 248 วัน  │ 239 วัน  │1840.4 ชม.│ 920.2 ชม.│  แถวตัวเลขสรุป (KPI)
└──────────┴──────────┴──────────┴──────────┘
┌─────────────────────┬─────────────────────┐
│ อัตราการเข้างาน      │ การลางาน            │  2 คอลัมน์ ~540 ต่อข้าง
│ (โดนัท + รายละเอียด) │ (สรุป + รายประเภท)   │
└─────────────────────┴─────────────────────┘
┌───────────────────────────────────────────┐
│ ชั่วโมงทำงาน (กราฟ)                        │  เต็มความกว้าง
└───────────────────────────────────────────┘
```

**ทำไมถึงได้ผล** — พอคอลัมน์แคบลงเหลือ ~540 ระยะจากชื่อประเภทลาถึงตัวเลข
ลดจาก 660px เหลือราว 180px ซึ่งใกล้เคียงกับบนมือถือที่อ่านรู้เรื่องอยู่แล้ว
ไม่ต้องออกแบบการ์ดใหม่เลย แค่เปลี่ยนที่วาง

**ตัวเลข 4 ตัวควรรวมเป็นแถวเดียว** ไม่ใช่เพราะสวย แต่เพราะตอนนี้ต้องเลื่อนจอ
ถึงจะเห็นครบ ทั้งที่มันคือชุดข้อมูลสรุปชุดเดียวกัน

**กราฟเต็มความกว้างได้** เพราะแท็บ สัปดาห์/เดือน จะมีแท่งเยอะกว่านี้มาก
แต่ต้องแก้เคสแท่งน้อย: จำกัดความกว้างสูงสุดต่อแท่งแล้วจัดกลุ่มไว้กลาง
ไม่ใช่กระจายเต็มพื้นที่

**ประเด็นที่ต้องตรวจกับข้อมูลจริง (อาจเป็นบั๊ก)**
- โดนัทแสดง ตรงเวลา 72% / สาย 9% / ขาด 5% รวมได้ 86% — ส่วนที่เหลืออีก 14%
  มีเป็นวงสีเทาในโดนัทแต่**ไม่มีในคำอธิบายสัญลักษณ์** ผู้ใช้จึงไม่รู้ว่ามันคืออะไร
  (174+23+14 = 211 วัน จาก "วันที่ต้องทำงานจริง" 239 วัน — หายไป 28 วัน)
  ต้องรู้ก่อนว่าคืออะไรถึงจะตัดสินได้ว่าควรเพิ่มเข้าคำอธิบายหรือเป็นบั๊กการคำนวณ

> 📐 ตอนลงมือแตะกราฟจริง ให้ใช้แนวทางจาก skill `dataviz` — โดยเฉพาะเรื่อง
> "ตัวเลขสรุปไม่ควรเป็นกราฟแท่ง ให้เป็น stat tile" และถ้าเปลี่ยนสีต้องรัน
> ตัวตรวจ palette ก่อน ไม่ใช้สายตาตัดสินว่าคนตาบอดสีแยกออกมั้ย

---

### 🔵 เข้าข่าย Phase 2 (master-detail) มากกว่า Phase 3

#### `/personnel-info` — ⭐ เคสที่ชัดที่สุด
**ที่เห็น**: ทั้งหน้ามีแค่ปุ่ม "เลือกบุคลากร" อันเดียว ที่เหลือว่างเปล่าทั้งจอ

**เสนอ**: ซ้ายเป็นรายชื่อบุคลากร (ค้นหาได้) ขวาเป็นข้อมูลของคนที่เลือก
บนมือถือคงพฤติกรรมเดิม (กดแล้ว push ไปหน้าใหม่)

#### `/settings/user-management`
**ที่เห็น**: รายชื่อผู้ใช้ ชื่อซ้าย–ป้ายตำแหน่งขวาสุด กดแล้ว push ไปหน้ารายละเอียด

**เสนอ**: รูปแบบเดียวกับ `personnel-info` — ถ้าทำสองหน้านี้ด้วยโครงเดียวกันได้
จะได้ของใช้ซ้ำสำหรับ `/approval` ต่อไปด้วย

---

### ⚪ ตัดสินใจเชิงโครงสร้าง ไม่ใช่ layout

#### `/settings` — ซ้ำกับ sidebar
บนโหมด `expanded` ทุกรายการในหน้านี้อยู่ใน sidebar อยู่แล้ว ยกเว้น "เพิ่มลายเซ็น"
กับ "ออกจากระบบ"

**ทางเลือก**
1. โหมด `expanded` โชว์เฉพาะสองรายการที่ไม่ซ้ำ
2. ตัดไอคอนเฟืองออกจากแถบโปรไฟล์ แล้วย้ายสองรายการนั้นไปเป็นเมนูของแถบโปรไฟล์
3. ปล่อยไว้ — ยอมให้ซ้ำ เพราะบางคนชอบมีหน้ารวม

ผมเอนไปทางข้อ 1 (เปลี่ยนน้อยสุด ได้ผลชัด) แต่ข้อ 2 สะอาดกว่าในระยะยาว

---

## บั๊กที่ต้องแก้ ไม่เกี่ยวกับการออกแบบ

| # | หน้า | อาการ |
|---|---|---|
| 1 | `/statistic` | ตัวเลขแกน Y ของกราฟตัดบรรทัด ("1,282" → "1,28" / "2") |
| 2 | `/settings/user-management` | แถว `admin` ไม่มีชื่อ |
| 3 | `/statistic` | โดนัทมีวงสีเทา ~14% ที่ไม่มีในคำอธิบายสัญลักษณ์ (ดูหัวข้อ statistic) |
| 4 | `/profile` | ฟิลด์ว่างเกือบหมด — ต้องเช็คกับบัญชีปกติก่อนว่าเป็นบั๊กหรือเป็นเพราะบัญชี `system-root` ไม่มีข้อมูลจริง |

---

## ลำดับที่แนะนำ

```
1. เคาะกติกา 4 ข้อ            ← ต้องได้ก่อน ไม่งั้นแก้ไปก็ไม่เป็นชุดเดียวกัน
2. ทำกติกาข้อ 1 ให้เป็นโค้ด    (แยก contentMaxWidth เป็น form/list/dashboard)
3. หน้ากลุ่ม 🟡 (8 หน้า)      แค่เปลี่ยนความกว้าง งานเบา เห็นผลเร็ว
4. บั๊ก 3 ข้อ                 งานเล็ก แยกจากงานออกแบบ
5. หน้ากลุ่ม 🔴 (4 หน้า)      ต้องออกแบบจริง ทำทีละหน้า
6. Phase 2 master-detail      personnel-info ก่อน แล้วค่อยขยายไป user-management
```

ข้อ 3 คุ้มสุด — 8 หน้าด้วยการเปลี่ยนตัวเลขความกว้าง ถ้าข้อ 2 ทำไว้ดี
