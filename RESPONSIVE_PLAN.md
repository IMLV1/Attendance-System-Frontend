# แผนออกแบบ layout iPad + Desktop ใหม่

> เอกสารทำงาน — แก้ในนี้ได้เลย ผมจะยึดไฟล์นี้เป็นหลักตอนลงมือ
> สถานะ: **Phase 0, 1, 4 เสร็จแล้ว** เหลือ Phase 2 (master-detail) กับ Phase 3 (รายหน้า)
> อัปเดต 24 ส.ค. 02:50

---

## ✅ ทำไปแล้ว

| วันที่ | งาน | commit | ผล |
|---|---|---|---|
| 23 ส.ค. | ช่วง 600–1200 ไม่มีเมนู → ใช้ bottom nav | `6c0551e` | iPad แนวตั้งมีเมนูแล้ว |
| 24 ส.ค. | **1.1** ไอคอน sidebar (`assets/images/` ไม่มีชื่อไฟล์) | `7001cca` | ✅ ยืนยันบน iPad จริง: `Unable to load asset` เหลือ **0 ครั้ง** |
| 24 ส.ค. | **1.2** ลิงก์ตาย 7 จุด + ตัด 3 ปุ่มที่ไม่มีหน้ารองรับ | `7001cca` | ✅ ตรวจแล้ว 13/13 path ตรงกับ `routes.dart` |
| 24 ส.ค. | **1.3** สิทธิ์จริงแทน `permissionLevel = 3` + **1.4** โปรไฟล์ + **1.5** ปุ่ม back | `954e251` | ✅ role `main` เห็น 7 เมนู (ไม่มีจัดการผู้ใช้/ตั้งค่าระบบ) |
| 24 ส.ค. | **Phase 0** `LayoutMode` + ถอด `textScaler` + จำกัดความกว้าง 1100 · และซ่อม sidebar ที่ว่างเปล่า | `32febbf` | ✅ iPad แนวตั้ง = bottom nav + hamburger, แนวนอน = sidebar ครบ |
| 24 ส.ค. | **Phase 4** `PopupSurface` — popup จอใหญ่เป็นกล่องกลางจอ | `6459784` | ⏳ คอมไพล์ผ่าน ยังไม่ได้กดดูด้วยตา |
| 24 ส.ค. | sidebar พับได้ (300 → 76) + ลดขนาดตัวหนังสือเมนู 20 → 16 | `4b81c7b` | ✅ เห็นปุ่มพับบน sim แล้ว |

**ลิงก์ที่แก้ใน 1.2**

| เดิม | ใหม่ |
|---|---|
| `/time-request` | `/attendance-request` |
| `/leave` | `/leave-request` |
| `/approval` | `/settings/approval` |
| `/personnel-info` | `/settings/personnel-info` |
| `/user-management` | `/settings/user-management` |
| `/role-management` | `/settings/role-management` |
| `/attendance-history` | `/settings/attendance-history` |

ตัดออก: `/leave-history`, `/time-request-history`, `/approval-history`
(ไม่มีหน้าจริง และประวัติอยู่ในหน้าหลักของแต่ละเรื่องแล้ว — ส่วน "รายการล่าสุด")

**ถัดไป**: ไล่ดู UI ทุกหน้าบน tablet/desktop (0.4) → กด popup ด้วยตา → Phase 3 → Phase 2
(ระบบไฟล์กับ splash พักไว้ก่อน — จดไว้ท้ายไฟล์แล้ว)

---

## 🐞 บั๊กที่เจอระหว่างทำ (จดไว้กันลืม)

**1. sidebar ว่างเปล่า เหลือแต่แถบโปรไฟล์**
ตอนใส่แถบโปรไฟล์ (1.4) เอาไปวางในช่อง `bottomNavigationBar` ของ `Scaffold`
ที่ซ้อนอยู่ใน sidebar ปรากฏว่าช่องนั้นกินความสูงทั้งคอลัมน์ (สังเกตได้จาก
แถบโปรไฟล์ไปอยู่กึ่งกลางแนวตั้ง) `body` ที่เป็นรายการเมนูจึงเหลือความสูง 0
→ เลิกใช้ `Scaffold` ซ้อน เปลี่ยนเป็น `Column` ตรงๆ: โลโก้ / `Expanded(ListView)` / โปรไฟล์

**2. hot restart ที่ไม่เคยเกิดขึ้นจริง**
สคริปต์ที่ใช้สั่ง hot restart ยิงปุ่มผ่าน `System Events keystroke` ซึ่งไปเข้า
แอปที่อยู่หน้าสุด — ตอนไปหมุนจอ Simulator แล้วมันขึ้นมาหน้าสุด ปุ่มเลยไปโดน
Simulator แทน Terminal ทำให้ตรวจงานผิดอยู่หลายรอบ (นึกว่าโค้ดไม่ทำงาน
ทั้งที่แค่ยังไม่ได้ restart) → เปลี่ยนไปใช้ `do script ... in selected tab`

**3. จอค้าง กดอะไรไม่ได้เลย ที่หน้าย่อยของ `/personnel-info`** (24 ส.ค., แก้แล้ว)
เจอทั้ง iPhone / iPad / web — เลือกบุคลากรแล้วกดเมนูย่อย (ข้อมูลส่วนตัว ฯลฯ)
แล้วทั้งจอไม่ตอบสนอง

ต้นเหตุ: `bf05e66` ทำให้ `Header.subHeader` เรียก `GoRouterState.of(context)`
**ตรงๆ ไม่มีตัวกัน** ซึ่ง go_router จะ **throw** (ไม่ใช่คืน null) เมื่อหน้านั้น
ไม่ได้มาจาก route ของ go_router — และมีหน้าที่ push ด้วย `MaterialPageRoute`
ตรงๆ อยู่ราวสิบหน้า (หน้าย่อย 5 ตัวของ personnel-info, `UserInfo`, `CreateUser`,
`AssignRole`, `EditRole`, `ConfigLeave`, `MaxLeave`, `SetMaxLeave`,
`LeaveApprovalDetail`, `OverallInfo`)

header สร้างไม่ขึ้น → ทั้งหน้าสร้างไม่ขึ้น → เหลือหน้าที่พังทับหน้าเดิมอยู่
โดยไม่มีปุ่ม back = ดูเหมือนจอค้าง

**แต่นั่นเป็นแค่ครึ่งเดียว** — แก้ให้ไม่ throw แล้ว (`e389fb2`) ยังค้างเหมือนเดิม
เพราะมีลูปซ้อนอยู่อีกชั้น: ข้างใน `GoRouterState.of()` ถ้าหา state จาก route
ปัจจุบันไม่เจอ มันจะ**ไล่ขึ้นไปตาม Navigator ทีละชั้น** แล้วเรียก
`dependOnInheritedWidgetOfExactType` บน `context` **ของ Navigator** ซึ่งไม่ใช่
context ของ widget ที่กำลัง build อยู่ = จับ Navigator ไปเป็น dependent ของ
registry → registry แจ้งเปลี่ยนทีนึง Navigator rebuild ทั้งสาย → หน้าเรา build
ใหม่ → ลงทะเบียนซ้ำ → **วนไม่จบ กิน CPU 100%** จน hot restart ก็ไม่ลง

แก้จริงที่ `Header._matchedLocation()` (`6b8d9a0`) — เช็ค
`ModalRoute.of(context)?.settings is Page` ก่อน route ของ go_router ใช้ `Page`
เสมอ ส่วนหน้าที่ push เองไม่ใช่ จึงคืน `null` ทันทีโดยไม่ต้องไล่ขึ้นไปไหน
(try/catch ยังคงไว้เป็นตาข่ายชั้นสอง) และหน้าที่ไม่มี matchedLocation ถือเป็น
หน้าลูกเสมอ (ต้องมีปุ่ม back) มี test กันไว้ที่
`test/header_imperative_route_test.dart` — ก่อนแก้ค้างเกิน 7 นาที หลังแก้ 4 วิ

### วิธีจับ (จดไว้ใช้ซ้ำ)
1. console บอกก่อนเลย: `Performing hot restart...` ค้างหมุนไม่จบ = isolate ตาย
2. `ps aux | grep Runner` → CPU 100% ค้าง = ลูป ไม่ใช่ปัญหา layout
3. `sample <pid>` ได้แต่ address ของ JIT ไม่มีชื่อ — **ใช้ไม่ได้กับ Dart**
4. ที่ใช้ได้คือ **Dart CPU profile ผ่าน VM service** (`getCpuSamples`) แล้วกรอง
   เฉพาะเฟรมที่ script เป็น `package:attendance_system` → ชี้บรรทัดได้ทันที
   (สคริปต์อยู่ที่ scratchpad ของ session นี้ ถ้าจะใช้อีกต้องเขียนใหม่)
   ⚠️ ต้องดึงตอน **ไม่มี hot restart ค้างคา** ไม่งั้น DDS ไม่ตอบ

> ทางแก้จริงคือย้ายหน้าพวกนี้เข้า go_router — ไปรวมกับ Phase 2

---

## 🔬 หลักฐานจากเครื่องจริง (24 ส.ค.)

รัน `flutter run` บน **downn_fall's iPad** (ต่อสาย, `--dart-define=API_BASE_URL=http://192.168.1.132:3000`)
แอปขึ้นและคุยกับ backend ได้ปกติ แต่คอนโซลยิง error รัวไม่หยุด:

```
[ERROR:flutter/runtime/dart_vm_initializer.cc(40)] Unhandled Exception:
Unable to load asset: "assets/images/".
The asset does not exist or has empty data.
  SvgLoader._load (package:flutter_svg/src/loaders.dart:158)
```

**สรุปได้ 2 อย่าง:**

1. **iPad เครื่องนี้กว้าง ≥ 1200** → เข้าเงื่อนไข `isDesktop` → เจอ **sidebar** (ไม่ใช่เคส "ไม่มีเมนู")
2. ยืนยันปัญหาข้อ 2 ในตารางข้างล่างว่าเกิดจริง ไม่ใช่แค่อ่านโค้ดแล้วเดา

→ **ตัวปัญหาที่ผู้ใช้เจอคือ Phase 1 ไม่ใช่ Phase 0** จึงสลับลำดับการทำ (ดูท้ายไฟล์)

---

## สรุปสภาพปัจจุบัน (สำรวจจากโค้ดจริง)

| # | ปัญหา | ไฟล์ | ความรุนแรง |
|---|---|---|---|
| 1 | ~~ช่วง 600–1200 ไม่มีเมนูเลย~~ **แก้แล้ว** `6c0551e` | `base_view.dart` | ~~critical~~ |
| 2 | ~~ไอคอน sidebar พังทุกปุ่ม~~ **แก้แล้ว** `7001cca` — เดิม — `SvgPicture.asset('assets/images/')` ไม่มีชื่อไฟล์ `pageIcon` ที่ส่งเข้ามาไม่เคยถูกใช้ **← ยืนยันจากเครื่องจริงแล้ว** | `sidebar_navigation.dart:185` | critical |
| 3 | ~~10 จาก 12 ลิงก์ใน sidebar ชี้ไปหน้าที่ไม่มีจริง~~ **แก้แล้ว** `7001cca` | `sidebar_navigation.dart:54-97` | critical |
| 4 | ~~`permissionLevel = 3` hardcode~~ **แก้แล้ว** `954e251` — ใช้ `MenuAccess` | `sidebar_navigation.dart:18` | สูง |
| 5 | ~~`textScaler` ×1.2 / ×1.4 แต่กล่อง fix ความสูง~~ **ถอดออกแล้ว** `32febbf` | `app_scaffold.dart:37` | สูง |
| 6 | ~~โค้ดจำกัดความกว้าง 1100 ถูก comment ทิ้ง~~ **เปิดแล้ว** `32febbf` | `app_scaffold.dart:46-58` | กลาง |
| 7 | ~~popup ทั้งหมดเป็น bottom sheet ทุกขนาดจอ~~ **แก้แล้ว** `6459784` — `PopupSurface` | popup 3 ไฟล์ | **สูง** |
| 8 | ~~popup hardcode ความสูง 88 จุด~~ **แก้แล้ว** `6459784` — ถือเป็นเพดาน ไม่ต้องไล่แก้ call site | 88 call sites | **สูง** |
| 9 | 45 จุดใช้ `width: double.infinity` | 33 หน้า | กลาง |
| 10 | ~~กดปุ่ม back แล้วแอปพัง~~ **แก้แล้ว** `954e251` — ใช้ go_router อย่างเดียว | `header.dart:41-51` | **critical** |
| 11 | ~~หน้าที่มาจาก sidebar ยังมีปุ่ม back~~ **แก้แล้ว** `954e251` — ถอยไม่ได้ = ไม่โชว์ปุ่ม | `header.dart` + 33 หน้า | สูง |
| 12 | ~~โปรไฟล์ sidebar เป็น placeholder~~ **แก้แล้ว** `954e251` — รูป+ชื่อ+ตำแหน่ง / ตั้งค่า / ออกจากระบบ | `sidebar_navigation.dart:100-120` | **สูง** |

**ขนาดงาน**: 52 ไฟล์ใน `lib/features`, 33 หน้าใช้ `AppScaffold`

### ลิงก์ตายใน sidebar (ข้อ 3)

| ปุ่มใน sidebar | route จริง |
|---|---|
| `/time-request` | `/attendance-request` |
| `/leave` | `/leave-request` |
| `/approval` | `/settings/approval` |
| `/personnel-info` | `/settings/personnel-info` |
| `/user-management` | `/settings/user-management` |
| `/role-management` | `/settings/role-management` |
| `/attendance-history` | `/settings/attendance-history` |
| `/leave-history` | ❌ ไม่มีหน้านี้ |
| `/time-request-history` | ❌ ไม่มีหน้านี้ |
| `/approval-history` | ❌ ไม่มีหน้านี้ |

ใช้ได้จริงแค่ `/check-in` กับ `/statistic`

---

## หลักการที่เคาะแล้ว

### 3 โหมด layout — ตัดสินจาก **ความกว้าง + แนวจอ**

| โหมด | เงื่อนไข | เมนู | เนื้อหา |
|---|---|---|---|
| `compact` | width < 600 | bottom nav | 1 คอลัมน์ เต็มกว้าง |
| `medium` | iPad แนวตั้ง | bottom nav | 1 คอลัมน์ จำกัดกว้าง |
| `expanded` | iPad แนวนอน + desktop | sidebar | 2 คอลัมน์ |

**ทำไมต้องเอา orientation มาร่วม**: iPad Pro 11" แนวนอน = 1194 ตกเป็น "tablet" ทั้งที่พื้นที่พอสำหรับ sidebar สบายๆ ถ้าใช้ความกว้างอย่างเดียวจะแบ่งผิดตลอด

### ตัดสินใจแล้ว
- ✅ iPad แนวนอน = sidebar / แนวตั้ง = bottom nav
- ✅ `textScaler` — **เอาออก** ใช้ขนาดเดียวทุกจอ

---

## Phase 0 — รากฐาน

> 3 ไฟล์ แต่กระทบทุกหน้าพร้อมกัน

- [x] **0.1** เขียน `Responsive` ใหม่ — คืน `enum LayoutMode { compact, medium, expanded }` แทน bool 3 ตัว, คิด `MediaQuery.orientation` ร่วมด้วย ✅ `32febbf`
  - ไฟล์: `lib/core/utils/responsive.dart`
  - เก็บ `isMobile/isTablet/isDesktop` ไว้ก่อนเพื่อไม่ให้ที่เรียกอยู่พัง แล้วค่อยทยอยย้าย
- [x] **0.2** ลบ `textScaler` ออกจาก `AppScaffold` ✅ `32febbf`
  - ไฟล์: `lib/shared/widgets/app_scaffold.dart:36-38`
  - ผลพลอยได้: หน้าที่เคยล้นบน iPad หายล้นทันที (รวมหน้า check-in ที่เพิ่งเอา scroll ออก)
- [x] **0.3** เปิดการจำกัดความกว้างเนื้อหา ✅ `32febbf`
  - ไฟล์: `lib/shared/widgets/app_scaffold.dart:46-58` (โค้ดมีอยู่แล้ว comment ไว้)
  - `compact`/`medium` = เต็มกว้าง, `expanded` = max **1100** จัดกลาง ← _ตัวเลขนี้แก้ได้_
- [ ] **0.4** ไล่ดูทุกหน้าด้วยตาหลังแก้ (ไม่มี test)

---

## Phase 1 — ซ่อม sidebar ให้ใช้ได้จริง

> 2–3 ไฟล์ · **จบ Phase 0+1 = iPad/desktop ใช้งานได้จริงแล้ว** แค่ยังไม่ได้ใช้พื้นที่จอเต็มที่

- [x] **1.1** แก้ไอคอน — ใช้ `pageIcon` ที่ส่งเข้ามาอยู่แล้ว (`sidebar_navigation.dart:185`) ✅ `7001cca`
- [x] **1.2** แก้ลิงก์ทั้งหมด + ตัด 3 หน้าที่ไม่มีจริงออก ✅ `7001cca`
- [x] **1.3** ดึงกฎสิทธิ์ออกมาเป็นตัวกลางตัวเดียว ✅ `954e251`
  - ตอนนี้กฎที่ **ถูกต้อง** อยู่ที่ `setting_page.dart:59-85` แล้ว:
    ```
    ดูข้อมูลบุคลากร / อนุมัติ : admin, hr, main, special
    อนุมัติคำขอ              : admin, main
    จัดการผู้ใช้ / ตำแหน่ง    : admin, hr
    ตั้งค่าระบบ              : admin
    ```
  - ทำเป็น `MenuAccess` ใช้ร่วมกันทั้ง sidebar + setting_page จะได้ไม่หลุดจากกันอีก
  - ข้อมูลมีพร้อมแล้วที่ `AuthState.user.roleType`
- [x] **1.4** เพิ่มโปรไฟล์ + ออกจากระบบใน sidebar ✅ `954e251`
  - ตอนนี้ `bottomNavigationBar` ของ sidebar เป็น **placeholder ดิบๆ**:
    `Text('PROFILE HERE', style: TextStyle(color: Colors.amber, fontSize: 30))`
    (`sidebar_navigation.dart:100-120`)
  - **ผลคือบน desktop เข้าโปรไฟล์ / ลายเซ็น / ออกจากระบบไม่ได้เลย**
    เพราะของพวกนี้อยู่ในหน้า `/settings` ซึ่ง sidebar ไม่มีปุ่มไปหา
  - ต้องมี: avatar + ชื่อ + ตำแหน่ง, กดแล้วไป `/profile`, และปุ่มออกจากระบบ
  - พิจารณาเพิ่มปุ่มไป `/settings` ด้วย (ลายเซ็นอยู่ในนั้น)

---

## Phase 1.5 — "หน้า destination" vs "หน้า sub-page" 🆕

> เพิ่ม 24 ส.ค. — เจอตอนทดสอบบน iPad จริงหลังแก้ 1.2

### อาการที่เจอ

**1) กดปุ่ม back แล้วแอปพัง**

```
Failed assertion: 'currentConfiguration.isNotEmpty'
  GoRouterDelegate._handlePopPageWithRouteMatch
  NavigatorState.pop
  Header.subHeader.<anonymous closure> (header.dart:44)
```

**2) หน้าที่ควรเป็น "ปลายทาง" ยังแต่งตัวเป็น "หน้าที่ถูก push"**

บนมือถือ: หน้าพวกนี้ต้องเข้า `/settings` ก่อนแล้วค่อยกดเข้าไป → เป็น sub-page → มีปุ่ม back สมเหตุสมผล

บน iPad/desktop: กด sidebar ครั้งเดียวถึงเลย → เป็น **ปลายทาง** ไม่ใช่ sub-page
แต่ UI ยังเป็น `Header.subHeader` (มีปุ่ม back, title กลางจอ) เหมือนถูก push มา

### สาเหตุ

`header.dart:41-51` ปนกัน 2 ระบบ:

```dart
leading: Navigator.of(context).canPop()          // ← ถาม Navigator ของ Flutter
    ? IconButton(onPressed: () => Navigator.of(context).pop())
    : context.canPop()                            // ← ถาม go_router
        ? IconButton(onPressed: () => context.pop())
        : null,
```

sidebar ใช้ `context.go()` ซึ่ง **แทนที่สแตกทั้งหมด** ไม่เหลือประวัติให้ถอย
→ `Navigator` ตอบว่าถอยได้ แต่ go_router มี match list ว่าง → assert แตก

> **หมายเหตุ**: บั๊กนี้มีมาก่อนแล้ว แต่ก่อนหน้านี้ลิงก์ sidebar ตายหมด
> ไม่มีใครเดินไปเจอ พอแก้ 1.2 ให้ลิงก์ใช้ได้ เส้นทางนี้เลยเปิดออกมา

### ทางเลือกในการแก้

**ตัวเลือก A — ขั้นต่ำ (แนะนำให้ทำก่อน)** · 1 ไฟล์

ให้ `header.dart` ใช้ระบบเดียว คือ `context.canPop()` / `context.pop()` ของ go_router เท่านั้น
- ถอยได้ → โชว์ปุ่ม back (เคส push จริง เช่น กดดูรายละเอียดบุคลากร)
- ถอยไม่ได้ → **ไม่โชว์ปุ่ม back** (เคสมาจาก sidebar)

แก้ทั้ง crash และเรื่อง "ปุ่ม back ที่ไม่ควรมี" พร้อมกัน

**ตัวเลือก B — แยก header 2 แบบให้ชัด** · 1 ไฟล์ + ไล่แก้ที่เรียก

เพิ่ม `Header.pageHeader` สำหรับหน้าปลายทาง (title ชิดซ้าย ไม่มี back)
แล้วให้แต่ละหน้าเลือกเองตาม `LayoutMode`:
- `compact` → `subHeader` (มี back)
- `expanded` → `pageHeader` (ไม่มี back เพราะ sidebar บอกตำแหน่งอยู่แล้ว)

ได้หน้าตาที่ถูกต้องกว่า A แต่ต้องแตะหลายหน้า

**ตัวเลือก C — ทำจริงจังตาม Phase 2**

หน้าที่เป็นรายละเอียดไม่ควรเป็นหน้าเต็มบน expanded แต่ควรเป็นคอลัมน์ขวา
→ ปัญหา "back button" หายไปเองเพราะไม่มีการ push

### ลำดับที่แนะนำ

- [x] **1.5** ทำ **ตัวเลือก A** ก่อน (แก้ crash ทันที เสี่ยงต่ำ) ✅ `954e251`
- [ ] ค่อยพิจารณา B ตอนทำ Phase 0 (มี `LayoutMode` ใช้แล้ว)
- [ ] C คือ Phase 2 อยู่แล้ว

---

## Phase 2 — Master-detail  ⚠️ ก้อนใหญ่สุด

> ~12–15 ไฟล์

หน้าที่ควรเป็น 2 คอลัมน์บน `expanded`:

| หน้า | ซ้าย | ขวา | เอามั้ย |
|---|---|---|---|
| อนุมัติคำขอ | list คำขอ | รายละเอียด + ปุ่มอนุมัติ | ☐ |
| ข้อมูลบุคลากร | list คน | แท็บข้อมูล/เข้างาน/ลา/สถิติ | ☐ |
| แจ้งเตือน | list | รายละเอียด | ☐ |
| ประวัติการลา | list | รายละเอียด | ☐ |
| ประวัติขอเวลา | list | รายละเอียด | ☐ |

_ติ๊กเฉพาะหน้าที่อยากทำ — ไม่จำเป็นต้องทำครบ_

**งานจริงคือรื้อ popup ให้เป็น widget ใช้ซ้ำได้** ตอนนี้เนื้อหาผูกแน่นกับกรอบ `DynamicPushPopup`/`PushPopup` ต้องแยกเป็น:

```
XxxDetailContent   ← เนื้อล้วน ไม่รู้จักกรอบ
   ├─ compact  → ยัดใน popup เหมือนเดิม
   └─ expanded → วางคอลัมน์ขวา
```

⚠️ Phase นี้ไปแตะไฟล์ที่เพิ่งทำ lazy render (sliver) — พลาดแล้วพังเงียบ (compile ผ่านแต่ scroll เพี้ยน)

---

## Phase 3 — หน้าที่ต้องออกแบบเฉพาะตัว

> 📄 **ข้อเสนอรายหน้าแบบละเอียดอยู่ที่ [PHASE3_PAGE_DESIGN.md](PHASE3_PAGE_DESIGN.md)**
> (เขียน 24 ส.ค. หลังไล่ดูครบ 18 หน้า) — มีกติกา 4 ข้อที่ต้องเคาะก่อนลงมือ
> และแบ่งหน้าเป็น 3 กลุ่ม: ต้องออกแบบใหม่ 4 หน้า / แค่หดตามกติกา 8 หน้า /
> เข้าข่าย Phase 2 อีก 2 หน้า

### สถานะ (24 ส.ค. — ลงมือแล้ว รอไล่ดูด้วยตาบนเครื่องจริง)

| งาน | ทำอะไร | commit |
|---|---|---|
| กติกาข้อ 1 | `ContentShape { form 600 / list 800 / dashboard 1100 }` + `AppScaffold.maxWidth` | `db2ad6d` |
| หน้ากลุ่ม 🟡 (8 หน้า) | ย้ายมาใช้ความกว้างตามรูปทรง + budget-year เหลือ 480 | `db2ad6d` |
| `/profile` 🔴 | 2 คอลัมน์บน expanded (การ์ดรูป+ชื่อ / รายการฟิลด์) | `55a8446` |
| บั๊ก 3 ตัว | แกน Y ตัดบรรทัด · แถว admin ไม่มีชื่อ · วงสีเทาไม่มีคำอธิบาย | `40e0b71` |
| `/check-in` 🔴 | เลิก `spaceBetween` บนจอที่มีที่เหลือ จัดกึ่งกลาง กว้าง 560 | `7933b66` |
| `/statistic` | อัตราการเข้างาน + การลางาน วางคู่กัน · จำกัดความกว้างกราฟตามจำนวนแท่ง | `6b47f7c` |
| `/settings` | บนจอที่มี sidebar โชว์เฉพาะ "ลายเซ็น" กับ "ออกจากระบบ" (ทางเลือกข้อ 1) | ↓ |

**ยังไม่ได้ทำ**

- [ ] **KPI 4 ตัวรวมเป็นแถวเดียว** ที่ `/statistic` — ตอนนี้ 2 ตัวอยู่ใน `WorkingDay`
      (จาก `StatisticModel`) อีก 2 ตัวอยู่ใน `WorkingHour` (จาก `WorkingHourModel`)
      คนละ widget คนละ request ต้องดึงออกมาเป็น stat tile กลางแล้วประกอบใน
      `statistic_page.dart` + เผื่อเคสที่ข้อมูลมาไม่พร้อมกัน (request แยกกัน)
- [ ] **บั๊ก #4** `/profile` ฟิลด์ว่างเกือบหมด — ต้องล็อกอินบัญชี**ปกติ** (ไม่ใช่
      `system-root`) ถึงจะรู้ว่าเป็นบั๊กหรือเป็นเพราะบัญชีนั้นไม่มีข้อมูลจริง
- [ ] **`/login`** — ยังไม่ได้ตรวจบนจอกว้าง

---

## Phase 4 — ระบบ popup (ฉบับละเอียด)

> เดิมเขียนไว้สั้นๆ ว่า "เปลี่ยน bottom sheet เป็น dialog"
> สำรวจแล้วพบว่ามี **2 ปัญหาคนละเรื่องทับกันอยู่** — แก้แค่เรื่องเดียวไม่พอ

### ปัญหา 4A — presentation mode ผิดบนจอกว้าง

bottom sheet เป็นภาษาของมือถือ พอจอกว้างขึ้นต้องเป็น dialog กลางจอ
ตอนนี้ทุกอย่างเป็น bottom sheet หมดไม่ว่าจอขนาดไหน

### ปัญหา 4B — ความสูง hardcode ⚠️ ตัวนี้ทำให้ "มือถือจอใหญ่ก็ไม่รอด"

```
88 call sites ส่ง maxHeight มาเอง
   53 ×  maxHeight: 700
   29 ×  maxHeight: 750
    4 ×  maxHeight: 400
    2 ×  maxHeight: 650
69 call sites ใช้ FlexFit.tight   ← บังคับให้สูงเท่านั้นเป๊ะ
```

700/750 คือความสูงที่จูนกับเครื่องอ้างอิงเครื่องเดียว:
- จอเตี้ยกว่านั้น → เนื้อหาโดนตัด
- จอสูงกว่านั้น → เหลือที่ว่างเป็นพืด / sheet ลอยแปลกๆ

**นี่คือเหตุผลที่มือถือจอใหญ่ก็พัง ไม่ใช่เรื่อง breakpoint**

### 🔑 ข่าวดี — จุดที่ต้องแก้มีน้อยมาก

```
125 จุด เรียก showPopup()
  3 จุด ที่สร้าง sheet จริง:
        push_popup.dart:39
        service_popup/service_popup.dart:52
        multi_page/dynamic_push_popup.dart:30
```

**แก้ 3 ไฟล์ → call site ทั้ง 125 จุดไม่ต้องแตะเลย**

### ทางแก้ — แยก "เนื้อหา" ออกจาก "ภาชนะ"

ตอนนี้ chrome ของ sheet ฝังอยู่ในตัว popup แต่ละคลาส
(`Align(bottomCenter)` + `borderRadius.vertical(top: 40)` + `width: double.infinity`)
เลยเปลี่ยนภาชนะไม่ได้

ทำตัวกลาง **`PopupSurface`** ตัวเดียว ให้เลือกภาชนะตามความกว้าง:

| ความกว้าง | ภาชนะ | หน้าตา |
|---|---|---|
| < 600 | bottom sheet | เต็มกว้าง มุมมนบน ลากปิดได้ |
| 600–1000 | **dialog กลางจอ** | กว้างสุด 560 มุมมนรอบด้าน |
| ≥ 1000 | dialog กลางจอ | กว้างสุด 640 |

_ตัวเลข 560 / 640 แก้ได้_

### แก้เรื่องความสูง (4B)

เปลี่ยนความหมายของ `maxHeight` จาก **"กฎ"** เป็น **"เพดาน"**:

```
สูงจริง = min( ความสูงเนื้อหา , maxHeight ที่ส่งมา , 90% ของจอ )
```

- จอเตี้ย → ไม่โดนตัด
- จอสูง → หดตามเนื้อหา ไม่เหลือที่ว่าง
- `FlexFit.tight` → โหมด dialog ไม่สนใจ (dialog ต้องกอดเนื้อหา)

✅ **88 call sites ยังคอมไพล์ผ่านหมด** เลขเดิมกลายเป็นเพดานแทนความสูงตายตัว

### ลำดับทำ (เสี่ยงน้อย → มาก)

- [x] **4.1** สร้าง `PopupSurface` + ตัวเลือกโหมดตาม `LayoutMode` — เสี่ยงต่ำ ✅ `6459784`
- [x] **4.2** ย้าย `PushPopup` (ใช้เยอะสุด ง่ายสุด) แล้วหยุดดูผล — เสี่ยงต่ำ ✅ `6459784`
- [x] **4.3** ย้าย `ServicePopup` + 4 ตัวลูก (number/option/text/signature) — เสี่ยงกลาง ✅ `6459784`
- [x] **4.4** ย้าย `DynamicPushPopup` (nested Navigator + PopupProvider + wizard หลายหน้า) — **เสี่ยงสูง** ✅ `6459784`
- [x] **4.5** เปลี่ยนความหมาย `maxHeight` เป็นเพดาน — เสี่ยงกลาง ✅ `6459784`
- [x] **4.6** เกณฑ์เลือกโหมด: แผ่นเลื่อนใช้เฉพาะ `compact` (มือถือ) — เสี่ยงต่ำ

### 🔁 4.6 — แก้เกณฑ์เลือกโหมด (24 ส.ค., รอบสอง)

4.1 ผูก `presentationOf` ไว้กับ `Responsive.showSidebar` ซึ่งเป็นจริงเฉพาะโหมด
`expanded` → **iPad แนวตั้ง (โหมด `medium`) จึงยังได้แผ่นเลื่อนกว้างเต็มจอ 834px**
ซึ่งเป็นอาการเดียวกับที่ตั้งใจย้ายมา `PopupSurface` เพื่อแก้ตั้งแต่แรก

เกณฑ์ที่ถูกคือ "จอเล็กแค่ไหน" ไม่ใช่ "มี sidebar รึเปล่า" — แผ่นเลื่อนเหมาะกับ
มือถือเพราะนิ้วโป้งเอื้อมถึงขอบล่างพอดี พอจอกว้างเกินนั้นกล่องกลางจออ่านง่ายกว่า
เสมอ ไม่เกี่ยวกับแนวจอ

| โหมด | เดิม | ตอนนี้ |
|---|---|---|
| `compact` (มือถือ ทุกแนว) | แผ่นเลื่อน | แผ่นเลื่อน (เหมือนเดิม) |
| `medium` (iPad แนวตั้ง) | **แผ่นเลื่อน** | **กล่องกลางจอ** |
| `expanded` (iPad แนวนอน / desktop) | กล่องกลางจอ | กล่องกลางจอ (เหมือนเดิม) |

แก้ที่เดียวใน `presentationOf` — call site ทั้งหมด (`PushPopup`, `ServicePopup`,
`DynamicPushPopup`) ถามผ่านตัวนี้อยู่แล้วจึงตามไปเอง รวมถึงกติกา `FlexFit.loose`
ของโหมดกล่องด้วย

### ⚠️ จุดที่ต้องระวังที่สุด — keyboard

กล่อง "ระบุเหตุผล" ในหน้าอนุมัติถูกแก้ไว้ด้วย `Stack` + `Positioned` ให้เด้งเหนือ
keyboard โดยไม่กระทบ popup ข้างหลัง

โค้ดนั้นเขียนบนสมมติฐานว่าเป็น **bottom sheet** พอเปลี่ยนเป็น dialog กลางจอ
Flutter จัดการ keyboard คนละแบบ (`viewInsets` vs dialog insets)

→ **ต้องรื้อดูใหม่ อาจไม่จำเป็นอีกต่อไป หรืออาจพัง** ทดสอบข้อนี้ทุกครั้งหลังแก้

### หมายเหตุ — ความสัมพันธ์กับ Phase 2

บนจอกว้าง popup บางตัว**ไม่ควรเป็น popup เลย** ควรเป็นคอลัมน์ขวาของ
master-detail (หน้าอนุมัติ, ข้อมูลบุคลากร) — นั่นคือ Phase 2

แต่ Phase 4 คือ 80/20 ที่แก้ทุกหน้าพร้อมกันด้วย 3 ไฟล์
**ทำ Phase 4 ก่อน Phase 2 คุ้มกว่า** และถ้าทำ Phase 2 ทีหลัง Phase 4 ก็ยังไม่เสียของ
(หน้าที่เหลือยังต้องใช้ popup อยู่ดี)

---

## ✅ Phase 4 — ผลตรวจด้วยตาบน iPad Pro 13" แนวนอน (24 ส.ค. 09:20)

รันบน simulator แนวนอน (1376×1032 → `expanded` → popup เป็นกล่องกลางจอ)

| popup | ผล |
|---|---|
| `PushPopup` (ตัวกรอง, `maxHeight: 750`) | ✅ กล่องกว้าง 560 **หดตามเนื้อหา** ~290 |
| `PushPopup` (เลือกวันที่+เวลา) | ✅ หดตามเนื้อหา และพอกางวงล้อเวลาก็โตจนชนเพดาน 0.82 แล้วเลื่อนต่อได้ |
| `ServicePopup` (ลายเซ็น) | ✅ กล่องกลางจอ หดตามเนื้อหา |
| `DynamicPushPopup` — `scroll: true` | ✅ หดจาก 750 เหลือ ~551 (หลังแก้ ดูข้างล่าง) |
| `DynamicPushPopup` — `scroll: false` | ➖ ยังเต็มความสูง — **ไม่ใช่บั๊ก** ดูข้างล่าง |

### wizard ต้องแก้คนละแบบกับอีกสองตัว

`Flexible` ห่อ `Navigator` อยู่ ซึ่ง `Overlay` → `_RenderTheatre` วางตัวเป็น
`constraints.biggest` เสมอ **เปลี่ยน `FlexFit` เป็น `loose` จึงไม่ช่วยเลย**
(วิธีที่ใช้ได้กับ `PushPopup` / `ServicePopup` ใช้กับตัวนี้ไม่ได้)

ทางแก้: เพิ่ม `_MeasureHeight` (`RenderProxyBox`) วัดความสูงเนื้อหาจริงแล้วป้อนกลับ
เข้า `SizedBox` — เข้ากับดีไซน์เดิมที่มี `AnimatedContainer` ไล่ความสูงตาม config อยู่แล้ว

### ทำไม `scroll: false` ถึงไม่แก้

วัดได้เฉพาะหน้าที่เนื้อหาอยู่ใน `SingleChildScrollView` (ความสูงไม่จำกัด → ได้ขนาดจริง)
ส่วนหน้าที่ปิด `scroll` เช่น `attendance_detail_popup.dart:67` ใช้ `Stack` +
`Positioned.fill` เพื่อตรึงแถบปุ่มอนุมัติ/ปฏิเสธไว้ขอบล่างไม่ให้ขยับตามคีย์บอร์ด —
**ตั้งใจให้เต็มความสูง** ไม่มีความสูงตามธรรมชาติให้วัด ถ้าฝืนวัดจะได้ค่าเท่าเพดานเดิม
(วัดเงาตัวเอง)

ที่ว่างที่เห็นตอนเปิดคำขอที่อนุมัติไปแล้ว เกิดจากแถบปุ่มไม่ถูก render — เป็นเรื่อง
**ออกแบบรายหน้าบนจอกว้าง (Phase 3)** ไม่ใช่แก้ที่เปลือก popup

สัดส่วน: 12 จุดที่เรียก wizard → **8 จุด `scroll: false`** / 4 จุด `scroll: true`

### หมายเหตุการทดสอบ

- บัญชี `attendance.approval@eng.src.ku.ac.th` ไม่มีคำขอของตัวเองเลย ต้องสร้างคำขอทดสอบ
  **REQ000000000012** (20 ส.ค. 2569, 06:00–12:00) ถึงจะกด wizard ที่ `scroll: true` ได้
  — ยังค้างอยู่ใน dev DB กดยกเลิกในแอปได้ถ้าไม่ต้องการแล้ว
- `main.dart:40` `setPreferredOrientations([portraitUp])` **ไม่มีผลบน iPad** เพราะแอป
  รองรับ multitasking (ไม่มี `UIRequiresFullScreen`) iOS ปฏิเสธทิ้ง — เห็นเป็น log
  `Failed to change device orientation ... UISceneErrorDomain Code=101` ทุกครั้งที่เปิดแอป

### 🔍 หมุนจอบนมือถือ — เทสแล้ว (iPhone 17 Pro, 24 ส.ค. 09:25)

สั่ง Rotate Left ด้วยวิธีเดียวกับที่หมุน iPad สำเร็จ ผล: **แอปค้างแนวตั้ง ไม่หมุนตาม**
และ log ไม่มีบรรทัด `Failed to change device orientation` เลยสักครั้ง (ต่างจาก iPad
ที่ขึ้นทุกรอบ) → บนมือถือ iOS **ยอมรับ** การล็อกแนวตั้ง

**ผลต่อโค้ด**: `Responsive._sidebarMinHeight` (กันเคส "มือถือจอใหญ่หมุนแนวนอน เช่น
iPhone Pro Max 956×440") **เป็น dead code บน iOS** เพราะมือถือไปแนวนอนไม่ได้เลย
— ยังมีประโยชน์อยู่บน **web** ที่ผู้ใช้ย่อ/ขยายหน้าต่างได้อิสระ จึงยังไม่ต้องถอดออก
แต่เวลาเทสช่วง `medium` ต้องเทสบน web หรือ iPad แนวตั้ง ไม่ใช่มือถือ

---

## 🖥️ desktop = web (สรุป 24 ส.ค. 10:00)

**ไม่มี native desktop** — ไม่มีโฟลเดอร์ `macos/` `windows/` `linux/` โปรเจกต์รองรับ
แค่ android / ios / web ที่ `flutter devices` โชว์ `macOS (desktop)` เป็นเพราะ toolchain
ของเครื่อง ไม่ใช่ของโปรเจกต์ ถ้าจะทำต้อง `flutter create --platforms=macos .` ก่อน
และ plugin ที่ขาดบน macOS มีตัวเดียวคือ `flutter_file_dialog` (= งาน Phase 5)

**คำสั่งรัน**: `scratchpad/run_web.sh` → `flutter run -d chrome --web-port=5050`

⚠️ **ห้ามใช้พอร์ต 5000** — macOS เปิด AirPlay Receiver ค้างไว้ (`ControlCenter`)

### ต้องตั้งใน Google Cloud Console ก่อน (project 264137744621)

1. OAuth client ชนิด **Web application** (ตัว iOS ใช้กับ web ไม่ได้)
2. **Authorized JavaScript origins** = `http://localhost:5050` (+ `http://127.0.0.1:5050`)
   — ช่อง redirect URIs เว้นว่าง GIS แบบ popup ตรวจแค่ origin
3. เปิด **People API** — plugin บน web ยิง `content-people.googleapis.com/v1/people/me`
   เพื่อดึงชื่อ/อีเมล/รูป (`google_sign_in_web/lib/src/people.dart:19`)
4. ใส่ client id ที่ `.env` → `GOOGLE_CLIENT_ID_WEB` เท่านั้น
   **ไม่ต้องแตะ `web/index.html`** — `google_sign_in_web.dart:142` ให้ค่าจาก
   constructor ชนะ meta tag และ `.env` อยู่ใน `.gitignore` ด้วย

### 🐞 บั๊กที่เจอตอนเปิด web ครั้งแรก (แก้แล้ว)

**backend CORS ไม่อนุญาต `X-Client-Platform`** → เบราว์เซอร์บล็อกทุก request ทิ้ง
ตั้งแต่ preflight ยิง API ไม่ได้เลยสักเส้น
แก้ที่ `Attendance-System-Backend` commit `1841471`

**ทำไมกว่าจะเจอถึงนาน** — dio บน web รายงาน CORS rejection เป็น
`DioExceptionType.connectionError` ตัวเดียวกับตอนต่อ server ไม่ติดจริงๆ
`auth_state.dart:115` จึงขึ้นข้อความ "เชื่อมต่อเซิร์ฟเวอร์ไม่ได้" ซึ่งชี้ผิดทาง

> 💡 **ยังไม่ได้ทำ**: น่าเพิ่มการแยกแยะใน `_loginErrorMessage` ว่าถ้าเป็น
> `kIsWeb && connectionError` ให้บอกใบ้เรื่อง CORS ด้วย เพราะบน web สองสาเหตุนี้
> หน้าตาเหมือนกันเป๊ะ แต่วิธีแก้คนละเรื่อง

### ข้อสังเกตอื่นบน web

- **ฟอนต์ไทยมาช้า** — แอปใช้ `Inter` ซึ่งไม่มี glyph ไทย บนมือถือระบบมีฟอนต์สำรองให้
  แต่บน web CanvasKit ต้องโหลด Noto ไทยตอน runtime เฟรมแรกจึงเห็นเป็นกล่อง ▯ แว่บนึง
  — เกี่ยวโดยตรงกับหัวข้อ splash/จอขาวตอนเปิดข้างล่าง
- **ต้องเปิด Docker ไว้** ถ้าปิดเพื่อประหยัด RAM backend จะดับตาม ไม่ใช่บั๊กของแอป

---

## 📋 0.4 — ผลไล่ดู UI บน desktop (24 ส.ค. 10:40)

> ⚠️ **ผลรอบนี้ล้าสมัยไปแล้วบางส่วน** Phase 3 เปลี่ยนหน้าไปเยอะหลังจากนี้
> ดูรอบสาม (25 ส.ค.) ท้ายหัวข้อนี้ ซึ่งไล่ครบทุก route อีกครั้งและเจอบั๊ก
> ที่ Phase 3 ทำพังเอง 2 ตัว

รันบน web `-d web-server` viewport **1727×829** = โหมด `expanded` (sidebar)
บัญชี `admin` เห็นเมนูครบ 13 รายการ

### หน้าที่ดูแล้ว

| หน้า | ผล | ประเภทงาน |
|---|---|---|
| `/check-in` | ช่องว่างแนวตั้งกลางจอใหญ่มาก — `spaceBetween` ดันวงกลมสถานะกับการ์ดล่างไปคนละขอบ | Phase 3 |
| `/statistic` | ใช้พื้นที่ได้ดี **แต่ตัวเลขแกน Y ของกราฟตัดบรรทัด** ("1,282" → "1,28" / "2") | 🐞 บั๊ก |
| `/settings/attendance-history` | เนื้อหาแต่ละแถวกองซ้ายราว 40% ปล่อยว่างขวาเกินครึ่ง | Phase 3 |
| `/settings/personnel-info` | **โล่งทั้งหน้า** มีแค่ตัวเลือก "เลือกบุคลากร" ที่เหลือว่างเปล่า | ⭐ Phase 2 |
| `/settings/user-management` | ชื่อซ้าย–ป้ายตำแหน่งขวา ห่างกันมาก · **แถว admin ไม่มีชื่อ** | Phase 3 + 🐞 |
| `/settings/role-management` | ปกติดี | — |
| `/settings/budget-year` | **วงล้อเลือกเดือนถูกยืดเต็ม 1100px** ชื่อเดือนกับตัวเลขอยู่คนละฝั่งจอ | Phase 3 |
| `/settings/config-attendance` | แถวกว้าง 1100 ค่าอยู่ขวาสุด ช่องว่างกลางเยอะ | Phase 3 (เบา) |
| `/settings/config-attendance-request` | เหมือนข้างบน | Phase 3 (เบา) |

### 🔁 ปัญหาที่เจอซ้ำทุกหน้าใต้ `/settings`

**header คนละแบบกับหน้าหลัก** — หน้าหลัก (`check-in`, `statistic`, `attendance-request`)
มีไอคอน + ชื่อไทย/อังกฤษ + กระดิ่งแจ้งเตือน ส่วนหน้าใต้ `/settings` มีแค่ชื่อไทยจัดกลาง
ไม่มีไอคอน ไม่มีชื่ออังกฤษ ไม่มีกระดิ่ง — ดูเหมือนคนละแอป
**แก้ที่เดียวจบทุกหน้า** ถ้ารวม header ให้เป็นตัวเดียวกัน

### รอบสอง — 5 หน้าที่เหลือ (ดูบน release build)

| หน้า | ผล | ประเภทงาน |
|---|---|---|
| `/settings` | **ซ้ำกับ sidebar ทั้งหน้า** ทุกรายการมีใน sidebar อยู่แล้ว เหลือที่ไม่ซ้ำแค่ "เพิ่มลายเซ็น" กับ "ออกจากระบบ" | ⭐ โครงสร้าง |
| `/profile` | ฟิลด์ว่างเกือบหมด (ชื่อ-นามสกุล, Full-name, เพศ, สัญชาติ, เบอร์โทร) · การ์ดรูปเป็นแถบขาวเต็มจอที่มีแค่วงกลมเล็ก | 🐞 ข้อมูล + Phase 3 |
| `/notification` | แถวแจ้งเตือน ข้อความซ้าย–วันที่ขวาสุด ช่องว่างกลางเยอะ | Phase 3 (เบา) |
| `/settings/config-leave-type` | แถวกว้าง 1100 มีแค่ไอคอน+ชื่อซ้าย ลูกศรขวาสุด | Phase 3 (เบา) |
| `/leave-request/create` | หน้าเกือบว่าง มีแค่ตัวเลือกประเภทการลา แล้วปุ่ม "ส่ง" ไปติดขอบล่างจอห่าง ~600px | Phase 3 |

**ปุ่ม back ถูกต้องทุกหน้าที่ตรวจ** — `/notification` และ `/leave-request/create`
มีปุ่ม back (ถูก push มาจริง) ส่วนปลายทาง sidebar ทั้งหมดไม่มี

### ✅ สรุป 0.4 — ดูครบ 18/20 route

(`/splash` ไม่มีวันถูกเห็นตามที่จดไว้ท้ายไฟล์ · `/login` ตรวจตอนเทส Google บน web)

**บั๊กที่ต้องแก้ 3 จุด**
1. `/statistic` — ตัวเลขแกน Y ของกราฟตัดบรรทัด ("1,282" → "1,28"/"2")
2. `/settings/user-management` — แถว `admin` ไม่มีชื่อ
3. `/profile` — ฟิลด์ว่างเกือบหมด (น่าจะเป็นข้อมูลของบัญชี system-root ไม่ใช่บั๊ก UI)

**ที่แก้ไปแล้ว**
- header ของหน้าใต้ `/settings` ไม่ตรงกับหน้าหลัก → commit `bf05e66`

**รูปแบบที่เจอซ้ำเกือบทุกหน้า** — แถวเนื้อหากว้างเต็ม 1100 แต่มีของอยู่แค่ซ้ายสุด
กับขวาสุด ช่องว่างกลางเยอะมาก เป็นผลจากการเอาเลย์เอาต์มือถือ (ที่กว้าง ~390)
มายืดตรงๆ — นี่คือแก่นของ Phase 3 ไม่ใช่บั๊กรายหน้า

### 📌 บันทึกวิธีทำงานกับ web

- **`-d chrome` ใช้ได้ แต่ต้องอยู่ในแท็บที่มันเปิดให้เท่านั้น** (แก้บันทึกเดิมที่เขียนว่า
  ต้องใช้ `-d web-server` — ไม่จริง) `-d chrome` ผูก dwds debug connection ไว้กับแท็บ
  นั้นแท็บเดียว ปิดแท็บ = WebSocket ขาด (`WebSocketConnectionClosed` ในคอนโซล) เปิด
  แท็บใหม่เองก็ไม่ re-attach ได้จอขาวอย่างเดียว ต้อง `flutter run` ใหม่ ห้าม refresh
- **อย่าสั่ง reload/เปลี่ยน URL ระหว่าง hot restart กำลังทำงาน** — connection จะค้างที่
  `Performing hot restart...` ตลอดกาล ต้อง kill process ทิ้งอย่างเดียว
- **ตั้ง profile ถาวรให้ Chrome** เพื่อไม่ต้องล็อกอิน Google ใหม่ทุกครั้งที่รีสตาร์ท
  (flutter สร้าง temp profile ใหม่ทุกครั้งถ้าไม่บอก):
  `flutter run -d chrome --web-port=5050 --web-browser-flag="--user-data-dir=<path>"`
- **deep link ใช้ได้แล้ว** ตั้งแต่ commit `f8310f4` — เปิด `localhost:5050/<path>` ตรงได้เลย
  ไม่ต้องคลิก sidebar ไล่ทีละหน้า (เดิมเด้งกลับ `/check-in` ทุกครั้งเพราะ MaterialApp ซ้อน)
- **debug build โหลด > 1000 request ต่อการเปิดหนึ่งครั้ง** (DDC เสิร์ฟ Dart แต่ละ
  library เป็นไฟล์ JS แยก) การ reload ทีละหน้าจึงช้ามาก — ให้โหลดครั้งเดียวแล้ว
  เดินในแอปด้วยการคลิก sidebar แทน

---

## 📋 0.4 รอบสาม — ไล่ครบทุก route หลัง Phase 3 (25 ส.ค.)

Chrome `-d chrome` viewport **1680×965** = โหมด `expanded` บัญชี `admin`
ไล่ครบ **19/19 route ที่เข้าถึงด้วย URL ได้** (deep link ใช้ได้แล้ว)

### 🐞 บั๊กที่ Phase 3 ทำพังเอง — แก้แล้วทั้งคู่

| บั๊ก | อาการ | สาเหตุ | commit |
|---|---|---|---|
| **แอดมินเข้าหน้าตั้งค่าระบบไม่ได้เลย** บนจอที่มี sidebar | เมนู "จัดการผู้ใช้งานระบบ" "จัดการตำแหน่ง" และหน้าตั้งค่าระบบทั้ง 4 หน้า หายไปหมดบน iPad แนวนอน/desktop **ไม่มีทางเข้าถึงได้เลยสักทาง** | Phase 3 เขียน `onlyUniqueItems = Responsive.showSidebar(context)` แล้วเอาไปซ่อน **ยกชุด** ทุกกลุ่ม โดยเข้าใจผิดว่าทุกรายการในหน้านี้อยู่ใน sidebar หมด ความจริง `sidebarDestinationList` มีแค่ 9 ปลายทางและ**ไม่มีหน้า admin เลยสักหน้า** | `f8310f4` |
| **`AppScaffold` เนื้อหาไม่สูงเต็ม viewport** | `/settings` พื้นหลังเทาสูงเท่าเนื้อหา ใต้การ์ดสุดท้ายเป็นขาวโล่ง · เป็นรูปเดียวกับที่ทำให้ iPad แนวตั้ง "เลื่อนแล้วเหมือนโดนตัด" | `Align` ส่ง constraint แบบหลวม (`minHeight: 0`) `Column`/`ListView` ยืดเองอยู่แล้วเลยไม่มีใครสังเกต แต่ `SingleChildScrollView` หดตามเนื้อหา | `5987174` |

**บทเรียน**: การ dedup เมนูต้องเทียบ **ราย path กับรายการจริง** ไม่ใช่เหมาว่า
"มี sidebar แล้วซ่อนหมด" — และเมื่อไหร่ที่เขียนเงื่อนไขซ่อน UI ตาม breakpoint
ต้องเปิดดูทั้งสองฝั่งของ breakpoint เสมอ

### 🐞 บั๊กอื่นที่เจอระหว่างทาง — แก้แล้ว

| บั๊ก | สาเหตุ | commit |
|---|---|---|
| **deep link / กด F5 เด้งกลับ `/check-in` ทุกครั้ง** | `main.dart` มี `MaterialApp` ครอบ `App()` ทั้งที่ `App` เองเป็น `MaterialApp.router` — Navigator ตัวนอกรับ initial route จาก engine แล้วกินทิ้ง (คอนโซลขึ้น `Could not navigate to initial route`) go_router ตกไปใช้ `initialLocation` ทุกครั้ง | `f8310f4` |
| **ลายเซ็นหายทุกครั้งที่หน้าตั้งค่า rebuild (เช่นหมุนจอ)** | `SettingPage` เป็น `StatelessWidget` แต่เก็บลายเซ็นไว้ในตัวแปร local ของ `build()` พอ rebuild ตัวแปรเกิดใหม่เป็น null แต่ `State` ของ `StatefulBuilder`/`ServiceUpdater` ยังเป็นตัวเดิม `fetchOnInit` เลยไม่ยิงซ้ำ | `d6d2abe` |

### 📐 ที่ปรับ

- `/check-in` โหมด `medium` (iPad แนวตั้ง) กว้าง 700 → **860** — บนจอ 1032 ที่ 700
  เหลือขอบข้างละ ~165 ดูโล่งเหมือนลืมจัด ที่ 860 เหลือข้างละ ~85 อ่านเป็นขอบที่ตั้งใจ
  **ไม่ปล่อยเต็มจอ** เพราะ layout แนวตั้งเรียงกล่องซ้อนกัน ยืดเต็มแล้วป้ายกับค่าจะห่างกัน
  เกือบ 1000 ซึ่งเป็นอาการเดิมที่ Phase 3 ไล่แก้มาทั้งงาน (`9cd4721`)

### 📝 ข้อสังเกตที่ยังไม่ได้แก้ (เรียงตามน้ำหนัก)

| # | หน้า | ปัญหา | ประเภท |
|---|---|---|---|
| **A** | `/personnel-info` | บนจอ 1680 มีแค่แถวเดียวว่า "เลือกบุคลากร" **ที่เหลือว่าง ~90%** — โล่งที่สุดในระบบ | ⭐ Phase 2 master-detail |
| **B** | `/statistic` | KPI 4 ตัวแยกเป็นสองก้อนคนละหัวคนละท้ายหน้า (วันทำงาน 2 ตัวบนสุด · ชั่วโมงทำงาน 2 ตัวล่างสุด เพราะมาจากคนละ model) · กราฟกว้าง ~980 แต่มี 2 แท่ง · แกน x เขียน `68`/`69` (พ.ศ. ย่อ) อ่านแล้วงง | Phase 3 |
| **C** | `/profile` | ฟิลด์ว่าง 5 ช่อง และช่อง "เลขบัตรประจำตัวประชาชน" โชว์ `system-root` (เอา `user_id` มาลงผิดช่อง?) — ต้องเทสด้วยบัญชีปกติถึงจะสรุปได้ | 🐞 ต้องยืนยัน |
| **D** | หน้าฟอร์มทุกหน้า | ปุ่ม "ส่ง"/"บันทึก" ติดก้นจอ ห่างจากฟอร์ม ~350px บน desktop | Phase 3 (เบา) |
| **E** | หน้าลูกของ `/settings` | ยังใช้ `subHeader` (ชื่อไทยกลางจอ ไม่มีไอคอน/ชื่ออังกฤษ) ต่างจากหน้าหลัก — **ปัญหาเดิมจากรอบแรกที่ยังไม่หาย** | Phase 3 |
| **F** | `/settings/config-attendance-request` | คำไม่ตรงกัน เมนูเขียน "คำขออนุมัติเวลางาน" แต่หัวหน้าเขียน "ตั้งค่าการขออนุมัติลา" | 🐞 ข้อความ |
| **G** | `/attendance-history`, `/settings/user-management` | การ์ดกว้าง 800 แต่เนื้อหาจบที่ ~590 เหลือขวาว่าง | Phase 3 (เบา) |
| **H** | `/settings/budget-year` | wheel เลือกเดือนเปิดค้างมาตั้งแต่แรก และขอบล่างการ์ดตัดพอดีกลางแถว | Phase 3 (เบา) |

### ✅ ที่ตรวจแล้วไม่มีปัญหา

`/login` (incognito) · `/notification` · `/approval` · `/leave-request` ·
`/attendance-request` · `/settings/role-management` · `/settings/config-leave-type` ·
`/check-in`

**ยังไม่ได้ดู**: หน้าที่ push แบบ imperative (รายละเอียดบุคลากร, สร้างผู้ใช้, แก้ role ฯลฯ)
deep link ไม่ถึง ต้องไล่กดจริง

---

## 🧭 sidebar เหลือแต่งานประจำวัน (24 ส.ค. 10:50)

เคาะแล้วว่า **ตัด 6 รายการออกจาก sidebar** ไปอยู่หลังไอคอนเฟือง (`/settings`) แทน
เพราะเป็นของที่ตั้งครั้งเดียวแล้วแทบไม่แตะอีก ไม่ใช่งานที่ทำทุกวัน

| เหลือใน sidebar (7) | ย้ายไปหลังไอคอนเฟือง (6) |
|---|---|
| ลงชื่อเข้า-ออกงาน | จัดการผู้ใช้งานระบบ |
| ขออนุมัติเวลาเข้า-ออกงาน | จัดการตำแหน่ง |
| การลางาน | ตั้งค่าปีงบประมาณ |
| สถิติ | การลงชื่อเข้า-ออกงาน |
| บันทึกการเข้างาน | คำขออนุมัติเวลางาน |
| อนุมัติคำขอ | ประเภทการลางาน |
| ข้อมูลบุคลากรในองค์กร | |

**สิทธิ์ไม่หายไปไหน** — `setting_page.dart:77,89` กัน `canManageUsers` /
`canConfigSystem` ด้วย `MenuAccess` ชุดเดียวกับที่ sidebar เคยใช้

**ผลพลอยได้ที่ตั้งใจ**: 6 หน้านั้นไม่ใช่ "ปลายทาง" อีกต่อไป จึงกลับไปมีปุ่ม back
และใช้ `subHeader` ซึ่งถูกต้องตามบริบทใหม่ — เป็นหน้าลูกของ `/settings` เหมือนกัน
หมดทุกจอ ไม่ต้องแยกพฤติกรรมมือถือ/desktop อีก

### 🆕 ตามด้วย: 3 หน้าที่เหลือถูกยกขึ้นเป็น route ระดับบนสุด

เจอว่า `บันทึกการเข้างาน` / `อนุมัติคำขอ` / `ข้อมูลบุคลากรในองค์กร` ยังเป็น
**route ลูกของ `/settings`** ทั้งที่อยู่ในเมนูข้างเหมือนหน้าหลัก พอ `context.go()`
ไป go_router สร้างสแตกที่มี `/settings` คาอยู่ข้างล่าง พฤติกรรมจึงไม่เหมือนกัน

ย้ายเป็น top-level แล้ว: `/attendance-history` · `/approval` · `/personnel-info`
(หน้าตั้งค่ายังเข้าถึงได้เหมือนเดิมเพราะนำทางด้วย `pushNamed` ไม่ใช่ path —
อ้าง path ตรงๆ มีแค่ 6 จุด)

และเติม **ชื่ออังกฤษ** ลงตารางปลายทาง (`nameEn`) เพราะแถบหัวของหน้าหลักทุกหน้า
มีสองบรรทัด (ไทย/อังกฤษ) แต่ 3 หน้านี้มีบรรทัดเดียว ทำให้ความสูงไม่เท่ากันและ
ดูเป็นคนละชุด — ตอนนี้ `Approval` / `Attendance History` / `Personnel Info` ครบแล้ว

**ที่ยังเหลือให้ตัดสิน**: หน้า `/settings` ยังลิสต์ของที่อยู่ใน sidebar ด้วย
(บันทึกการเข้างาน, อนุมัติคำขอ, ข้อมูลบุคลากร) ซึ่งบนมือถือจำเป็น (ไม่มี sidebar)
แต่บน desktop ซ้ำ — จะซ่อนเฉพาะโหมด `expanded` มั้ยยังไม่เคาะ

---

## Phase 5 — ระบบไฟล์: แนบ / พรีวิว / โหลด 🆕

> ⏸ **พักไว้ก่อน** (เคาะ 24 ส.ค.) — ให้ไปทำ UI ของ tablet/desktop รายหน้า (Phase 3) ให้จบก่อน
> เพราะยังมีอีกหลายเมนูที่หน้าตายังไม่เข้าที่ ส่วนระบบไฟล์ตอนนี้ "ใช้งานได้" อยู่แล้ว
> แค่ยังไม่ native เต็มที่ — ไม่ใช่ของพัง
>
> โจทย์: "อยากให้เป็น native ของแต่ละ platform"
> คำตอบสั้นๆ: **ได้ 2 ใน 3 ส่วน** ส่วนที่เหลือ native ไม่ได้เพราะ OS ไม่มีของให้ใช้ (ดูข้อ 5C)

### สภาพปัจจุบัน (สำรวจ 24 ส.ค.)

| ส่วน | ไฟล์ | ใช้อะไรอยู่ | native แล้วรึยัง |
|---|---|---|---|
| แนบไฟล์ | 4 หน้า (`*_create.dart`, `*_resend.dart` ของ leave/time-request) | `image_picker` + `file_picker` | ✅ ตัวเลือกไฟล์เป็นของ OS อยู่แล้ว — แต่**เมนู 3 ปุ่มเขียนเองและก๊อปซ้ำ 4 ที่** |
| พรีวิว | `file_preview_popup.dart` | `OverlayEntry` + `AnimationController` เขียนเอง, PDF ใช้ `pdfrx`, รูปใช้ `Image.network` + `InteractiveViewer` | ❌ ไม่ native เลย |
| ดาวน์โหลด | `downloader.dart` | web: `url_launcher` · มือถือ: โหลดลง temp แล้วเรียก `SharePlus.share()` | ⚠️ ครึ่งๆ — แชร์ ไม่ใช่ บันทึก |

### สิ่งที่เจอเพิ่มระหว่างสำรวจ

1. **`flutter_file_dialog: ^3.0.3` อยู่ใน pubspec แต่ไม่มีใครเรียกเลยสักบรรทัด**
   ทั้งที่มันคือตัวที่ควรใช้ในข้อ 5C พอดี (เปิด "บันทึกไปยังไฟล์" ของ iOS/Android)
2. **`path_provider` กับ `url_launcher` ถูก import ใน `downloader.dart` แต่ไม่ได้ประกาศใน `pubspec.yaml`**
   ตอนนี้ใช้ได้เพราะติดมากับ package อื่น (transitive) — วันไหน package นั้นเลิกพึ่ง จะพังทันที
   `flutter analyze` เตือนอยู่: `depend_on_referenced_packages`
3. `topGap = MediaQuery.padding.top + 3 * kToolbarHeight` ใน preview — เลขนี้เคาะจากมือถือ
   บน iPad จะเว้นหัวเยอะเกินจริง (เกี่ยวกับ Phase 0 โดยตรง)
4. เมนูแนบไฟล์มีปุ่ม **"ถ่ายรูป"** ซึ่งบน desktop/web ไม่มีกล้องให้ใช้ — ควรซ่อนตาม platform

---

### 5A — แนบไฟล์: ยุบเมนูที่ก๊อปซ้ำ 4 ที่ให้เหลือตัวเดียว

- [ ] **5.1** สร้าง `AttachmentPicker` ตัวกลาง (คืน `List<PlatformFile>`)
  - ย้าย logic ที่ซ้ำจาก 4 หน้าออกมา (แต่ละที่ ~90 บรรทัด)
  - รวมเรื่องตั้งชื่อ `IMG_xxxxx` + อ่าน bytes + แปลงเป็น `PlatformFile` ไว้ที่เดียว
  - ซ่อน "ถ่ายรูป" เมื่อไม่มีกล้อง, บนจอใหญ่แสดงเป็นเมนูข้างปุ่มแทนแผ่นเลื่อน
- [ ] **5.2** รองรับลากไฟล์มาวาง (drag & drop) บน desktop/web — `super_drag_and_drop`
  - อันนี้คือ "native ของ desktop" ตัวจริง ซึ่งตอนนี้ไม่มีเลย

### 5B — พรีวิว: เปลี่ยนไปใช้ตัวอ่านของ OS

ตรงนี้ทำเป็น native ได้จริง แต่**มีของแลก** ต้องเลือกก่อน:

| ทาง | ได้ | เสีย |
|---|---|---|
| **B1** เปิดด้วยแอปของระบบ (`open_filex` / `ACTION_VIEW` / QuickLook / แท็บใหม่บนเว็บ) | ท่าทางคุ้นมือ, สั่งพิมพ์/แชร์/ค้นข้อความได้ฟรี, ไม่ต้องดูแลโค้ด viewer เอง | **ออกจากแอป** (หรือมี sheet ของระบบทับ) คุมหน้าตาไม่ได้ ต้องโหลดไฟล์ลงเครื่องก่อนเสมอ |
| **B2** อยู่ใน `pdfrx` เหมือนเดิม แต่ซ่อมให้เข้ากับจอใหญ่ | คุมหน้าตาได้ ไม่ออกจากแอป | ยังต้องดูแล gesture/zoom เอง ไม่ได้ของแถมจากระบบ |
| **B3** ผสม — กดพรีวิว = ในแอป, มีปุ่ม "เปิดด้วยแอปอื่น" เพิ่ม | ได้ทั้งสองอย่าง | งานเยอะสุด |

- [ ] **5.3** เลือกทาง B1 / B2 / B3 ← _ยังไม่ได้เคาะ_
  - ความเห็นผม: **B3** แต่เริ่มจาก B2 ก่อน (แค่ซ่อม `topGap` + ทำเป็น `PopupSurface`)
    แล้วค่อยเติมปุ่ม "เปิดด้วยแอปอื่น" ทีหลัง — เพราะ B1 ล้วนๆ จะเสียความรู้สึกว่ายังอยู่ในแอป

### 5C — ดาวน์โหลด: อันนี้แหละที่ "native" ตีความได้หลายแบบ

**เรื่องที่ต้องเข้าใจก่อน**: iOS/Android สมัยใหม่**ไม่มี**โฟลเดอร์ Downloads ที่แอปเขียนลงไปตรงๆ ได้
ของที่เทียบเท่า "ดาวน์โหลด" ในแต่ละ platform คนละอย่างกันเลย:

| platform | ท่า native จริงๆ | ตอนนี้ทำอะไรอยู่ |
|---|---|---|
| iOS / iPadOS | เปิด **"บันทึกไปยังไฟล์"** (`flutter_file_dialog`) หรือ share sheet | share sheet ✅ ใกล้เคียงแล้ว |
| Android | **SAF** — ให้ผู้ใช้เลือกที่เก็บเอง (`flutter_file_dialog` เหมือนกัน) | share sheet ⚠️ ผิดความหมาย |
| Web | `<a download>` จริงๆ ให้เบราว์เซอร์โหลดเอง | `url_launcher` ⚠️ เปิดแท็บใหม่ ไม่ใช่โหลด |
| macOS / Windows | ไดอะล็อก "Save As" | ยังไม่รองรับ |

- [ ] **5.4** แยก **"แชร์"** ออกจาก **"บันทึก"** ให้เป็นคนละปุ่ม — ตอนนี้ปุ่มเดียวทำสองความหมายปนกัน
- [ ] **5.5** ต่อ `flutter_file_dialog` ที่มีอยู่แล้วเข้ากับปุ่ม "บันทึก" (iOS/Android)
- [ ] **5.6** web ใช้ anchor download แทน `url_launcher`
- [ ] **5.7** ประกาศ `path_provider` / `url_launcher` ใน `pubspec.yaml` ให้ถูกต้อง ← _ทำได้เลย เสี่ยง 0_

### ทำได้มั้ย — สรุป

**ได้** และ **5.7 กับ 5.5 เป็นของถูกมาก** (dependency มีครบแล้ว แค่ไม่เคยต่อสาย)
ตัวที่ต้องคุยก่อนคือ **5.3 (พรีวิว)** เพราะเป็นการแลกหน้าตาแอปกับความ native ตรงๆ

**ลำดับที่แนะนำ**: `5.7 → 5.5 → 5.6 → 5.4 → 5.1 → 5.3 → 5.2`
(ไล่จากของที่แทบไม่มีความเสี่ยง ไปหาของที่ต้องตัดสินใจเรื่องดีไซน์)

---

## 📌 splash ตอนเปิดแอป — จดไว้เฉยๆ ยังไม่ต้องทำ

### สรุปสั้น

ตอนนี้เปิดแอปแล้วเจอ **จอขาวเปล่าๆ ทุก platform** แล้วค่อยเด้งเข้าหน้า login/check-in
สาเหตุมี 2 ชั้นซ้อนกัน และ**ต้องแก้ทั้งคู่** ไม่งั้นยังเห็นขาวอยู่ดี:

| ชั้น | ปัญหา |
|---|---|
| 1. หน้าจอที่แสดง | ทั้ง iOS / Android / web ยังเป็น template เปล่าของ Flutter ที่ไม่เคยแก้ |
| 2. ระยะเวลาที่ค้าง | `main.dart` รอ **network 4 รอบเรียงกัน** ให้เสร็จก่อนถึงจะวาดอะไรออกจอ |

---

### ชั้นที่ 1 — ยังเป็น template เปล่า ไม่เคยแก้สักที่

| platform | ไฟล์ | สภาพ |
|---|---|---|
| iOS | `ios/Runner/Base.lproj/LaunchScreen.storyboard` + `LaunchImage.imageset` | รูป default ที่ Flutter สร้างให้ |
| Android | `android/app/src/main/res/drawable/launch_background.xml` | `<item android:drawable="@android:color/white" />` ส่วนที่ใส่รูปยัง**ถูก comment ทิ้งไว้** |
| Web | `web/index.html` | ไม่มี loading indicator เลย ขาวจนกว่า Flutter จะ boot เสร็จ |

`flutter_native_splash` generate ทั้งสามที่ให้จากรูปเดียวได้ (ใช้ `assets/images/` ที่มีอยู่)

---

### ชั้นที่ 2 — จอขาวค้างนานเพราะ boot รอ network

`main.dart:31-34`
```dart
await initializeDateFormatting('th_TH', null);
await dotenv.load(fileName: '.env');
await setupServiceLocator();
await getIt<AuthState>().init();   // ← ตัวปัญหา
runApp(...)                        // ← กว่าจะถึงบรรทัดนี้ จอขาวไปแล้ว
```

`AuthState.init()` ยิง API **เรียงกันทีละตัว ไม่ขนาน**:
`getProfile` → `ConfigLeave` → `ConfigAttendanceRequest` → `ConfigAttendanceTime`

= 4 round trip ต่อกัน **ก่อน** เฟรมแรกจะถูกวาด ยิ่งเน็ตช้ายิ่งขาวนาน

**แก้ได้ 2 ท่า (ทำพร้อมกันได้)**
- ยิง 4 ตัวนั้นแบบขนานด้วย `Future.wait` — ของถูก ได้ผลทันที
- ย้าย `init()` ออกมาไว้หลัง `runApp()` แล้วให้หน้า `/splash` เป็นตัวรอแทน

---

### ชั้นที่ 3 (ผลพลอยได้) — หน้า `/splash` ที่มีอยู่แล้วแต่ไม่มีวันถูกเห็น

`routes.dart:55-58`
```dart
GoRoute(
  name: '1',                       // ← ชื่อ route เป็นเลข 1 ด้วย
  path: '/splash',
  builder: (_, _) => const Center(child: CircularProgressIndicator()),
),
```

router จะพาไป `/splash` ก็ต่อเมื่อ `auth.status == AuthStatus.unknown`
แต่ `init()` ถูก await ก่อน `runApp()` (ชั้นที่ 2) พอ router เริ่มทำงาน
status เป็น authenticated/unauthenticated ไปแล้วเสมอ
→ **หน้านี้เป็นโค้ดตาย** ไม่มีใครเคยเห็น

ข่าวดีคือ ถ้าแก้ชั้นที่ 2 ตามที่เขียนไว้ (ย้าย `init()` ไปหลัง `runApp`)
route นี้จะ**กลับมามีชีวิตเอง** โดยไม่ต้องเดินสายใหม่ — เหลือแค่เปลี่ยน
`CircularProgressIndicator` เปล่าๆ เป็นหน้าจริง

---

### สรุปงานที่ต้องทำ (ยังไม่เริ่ม)

- [ ] **S1** ยิง 4 API ใน `AuthState.init()` แบบขนานด้วย `Future.wait` — เสี่ยงต่ำ เห็นผลทันที
- [ ] **S2** ย้าย `init()` ไปหลัง `runApp()` แล้วให้ `/splash` เป็นตัวรอ — ปลด route ที่ตายอยู่
- [ ] **S3** ทำ LaunchScreen ของ iOS / Android / web ให้ไม่ใช่จอขาว (`flutter_native_splash`)
- [ ] **S4** ทำหน้าตา LaunchScreen (ชั้น native) กับหน้า `/splash` (ชั้น Flutter) ให้ต่อกันเนียน
      ไม่งั้นจะเห็นรอยตัด: โลโก้นิ่ง → กระพริบ → โลโก้ที่ขยับได้

**ข้อควรรู้**: ชั้น native ใส่ animation ไม่ได้ ได้แค่รูปนิ่ง — อะไรที่ขยับได้ต้องอยู่ในชั้น Flutter เท่านั้น

---

## ความเสี่ยง

- Phase 0 กระทบ 33 หน้าพร้อมกัน และ **ไม่มี test เลยสักตัว** ต้องไล่ดูด้วยตา
- Phase 2 แตะโค้ด sliver ที่เพิ่งทำ — พังแบบเงียบได้
- Phase 4.4 (`DynamicPushPopup`) เป็นจุดเสี่ยงสูงสุดของงานทั้งหมด
- Phase 4 อาจทำให้โค้ดแก้ keyboard ที่เคยทำไว้พัง (ดูด้านบน)

---

## ยังต้องการคำตอบ

1. ~~**สกรีนช็อตบน iPad**~~ — ไม่จำเป็นแล้ว มีหลักฐานจากคอนโซลเครื่องจริงแทน
   (ถ้ามีก็ยังดี จะได้เห็นเรื่องความสวยงามที่คอนโซลบอกไม่ได้)
2. **Phase 2 เอาหน้าไหนบ้าง** → ติ๊กในตาราง Phase 2
   - ยังไม่ตอบ
3. **desktop = เว็บอย่างเดียวใช่มั้ย** หรือมีแผน macOS/Windows app
   - ตอบ:

---

## ลำดับที่แนะนำ (ปรับ 24 ส.ค.)

```
1.1 ✅ → 1.2 ✅ → 1.5 ✅ → 1.3 ✅ → 1.4 ✅ → Phase 0 ✅ → Phase 4 ✅ → หยุดดูผล ← อยู่ตรงนี้ → Phase 2/3
```

### เหลือทำ (ปรับ 25 ส.ค.)

| ลำดับ | งาน | ทำไม | สถานะ |
|---|---|---|---|
| ~~1~~ | **0.4** ไล่ดูทุกหน้าด้วยตาบน tablet/desktop | — | ✅ จบแล้ว ไล่ครบ 19/19 route (ดูหัวข้อ 0.4 รอบสาม) |
| ~~3~~ | **Phase 3** `/check-in` ออกแบบเฉพาะตัว | — | ✅ จบแล้ว (branch `phase3-page-layout` merge เข้า `main` แล้ว `9ea3c01`) |
| **1** | **กด popup บนจอใหญ่** | `PopupSurface` เปลี่ยนจาก bottom sheet เป็น dialog ตั้งแต่ Phase 3 แต่**ยังไม่เคยเห็นด้วยตาสักครั้ง** ตัวที่เสี่ยงสุดคือ `DynamicPushPopup` (wizard หลายขั้น) | ⬜ ยังไม่ทำ |
| **2** | **ข้อ C** `/profile` ฟิลด์ว่าง | ยังไม่รู้ว่าบั๊ก mapping หรือแค่บัญชี system-root — ต้องล็อกอินบัญชีปกติ | ⬜ ต้องพึ่งผู้ใช้ |
| **3** | **ข้อ F + H** คำไม่ตรงกัน / wheel เปิดค้าง | ของเล็ก รวมกันไม่ถึง 15 นาที | ⬜ ยังไม่ทำ |
| **4** | **ข้อ A** `/personnel-info` master-detail (= **Phase 2**) | โล่ง 90% บนจอกว้าง **และเป็นการแก้ถาวรของบั๊กจอค้าง** (หน้าที่ push แบบ imperative ไม่มีใน `routes.dart` → `GoRouterState.of` วน rebuild ไม่จบ) คุ้มที่สุดในลิสต์ | ⬜ ยังไม่ทำ |
| **5** | **ข้อ B** `/statistic` | หน้าเดียวที่ยังเป็น mobile ย่อจริงๆ — ต้องรวม KPI 4 ตัวที่แยกอยู่คนละหัวคนละท้ายหน้า | ⬜ ยังไม่ทำ |
| **6** | **ข้อ D, E, G** | ปุ่มติดก้นจอ / header ไม่สม่ำเสมอ / แถวอัดซ้าย — ทำทีเดียวรวดได้ | ⬜ ยังไม่ทำ |
| — | ~~Phase 5 ระบบไฟล์~~ | ⏸ **พักไว้** ยังใช้งานได้ ไม่ใช่ของพัง | — |
| — | ~~splash~~ | ⏸ **พักไว้** จดไว้เฉยๆ | — |
| — | ~~`S1` parallelize `AuthState.init()`~~ | ⏸ **พักไว้** | — |

**1.5 แทรกขึ้นมาก่อน 1.3** เพราะเป็น crash ที่ผู้ใช้เจอทันทีเมื่อกดปุ่ม back
และแก้ไฟล์เดียว (`header.dart`)

**ทำไม Phase 4 ขยับขึ้นมาก่อน Phase 2/3**: ปัญหา 4B (ความสูง hardcode)
กระทบ **ทุกเครื่องรวมถึงมือถือ** ไม่ใช่แค่ iPad/desktop — เป็นบั๊กที่ผู้ใช้เจออยู่ตอนนี้
และแก้แค่ 3 ไฟล์ก็ครอบคลุม popup ทั้งแอป (125 จุดเรียก)

**เดิมวางไว้ 0 → 1 แต่สลับเป็น 1 → 0** เพราะหลักฐานจากเครื่องจริงชี้ว่า
สิ่งที่ผู้ใช้เจอคือ sidebar พัง (Phase 1) ไม่ใช่เรื่อง breakpoint/ขนาดตัวอักษร (Phase 0)

เริ่มจากจุดที่เห็นผลเร็วที่สุด:

| ลำดับ | งาน | ขนาด | ผลที่เห็น |
|---|---|---|---|
| 1 | **1.1** แก้ไอคอน sidebar | 1 บรรทัด | error หยุดยิงทันที ไอคอนขึ้นครบ |
| 2 | **1.2** แก้ลิงก์ตาย 10 จุด | 1 ไฟล์ | กดเมนูแล้วไปถูกหน้า |
| 3 | **1.3** สิทธิ์จริงแทน hardcode | 2 ไฟล์ | ไม่เห็นเมนูที่ไม่มีสิทธิ์ |
| 4 | **1.4** เพิ่มโปรไฟล์/ออกจากระบบ | 1 ไฟล์ | ออกจากระบบได้บน desktop |
| 5 | **Phase 0** ทั้งหมด | 3 ไฟล์ | ตัวหนังสือไม่ล้น เนื้อหาไม่ยืด |

เหตุผลรวม: Phase 0+1 เป็นการ**ซ่อมของพัง** ยังไงก็ต้องทำ และได้ของใช้ได้จริงเร็ว
ส่วน Phase 2+ เป็นการ**เพิ่มคุณค่า** ซึ่งควรตัดสินหลังเห็นผลจริงบนเครื่อง
